# 本地验收记录（2026-09-04）

访问地址：http://127.0.0.1:9280/#/

验收账号：admin/admin123、limf/123456。

## 已完成

- 本地 npm ci 安装通过，JavaScript 语法检查通过。
- Node.js 22 应用镜像与 MySQL 8.0 容器启动，两个服务均 healthy。
- 单独创建全新数据库卷验证首次建表和种子初始化；测试环境已停止，验收环境保留运行。
- 25 项 HTTP 回归通过：登录、错误密码、未登录拦截、团队列表、团队切换、跨团队读写拦截、合法计划修改、团队上下文、关闭模拟免密入口、健康检查。
- 浏览器验证两个账号共 20 次页面访问，包括 limf 的 4 个无权限页面提示；管理员切换团队，计划列表隔离，负责人姓名及下拉选项正确；无浏览器异常或 API 错误。
- 应用容器重启后登录与团队回归通过，种子数据保留。
- 截图：artifacts/admin-plans.png、artifacts/limf-plans.png。
- 镜像：peis:local；可迁移镜像包：artifacts/peis-local.tar。
- 镜像包 SHA256：390D009897A9C8AD01274B4698E678B437EDAE65FB8067E73C6FD210FFD85B56。

## 修复

1. MySQL TEXT 默认值、唯一键、复合主键和索引字段类型不兼容。
2. team_members / teamMembers 表名不一致；首次初始化改为事务，失败不留下部分种子数据。
3. 团队越权时静默回退，以及按 ID 更新跨团队计划的漏洞；增加关联记录团队校验。
4. 新建种子账号使用 bcrypt 哈希存储，保留约定的验收密码。
5. limf 角色显示名与权限角色不匹配：回退解析团队成员角色，前端按权限显示菜单。
6. 普通成员的部门和人员选项改用当前团队上下文接口；OA 人员列表也按团队过滤。
7. 负责人姓名与人员选项误渲染 Promise。
8. Express 4 异步接口异常转交统一错误处理；增加数据库健康检查。

## 本次边界

本记录对应本地验收阶段，没有操作 192.168.3.110 服务器。验收后用户已要求将源码、容器配置与测试文档提交到 GitHub：haonanhu02-jpg/PEIS-，分支 main；服务器部署仍待后续执行。

OA、钉钉、致信仍是模拟实现，在容器中关闭。未验证真实企业登录、LDAP、消息推送、多人并发负载及生产备份恢复，不能将本次回归视为完整生产安全审计。

安装浏览器测试依赖时 npm 曾提示 3 项 moderate 漏洞。后续 `npm audit --omit=dev` 因官方审计接口超时未能取得明细，依赖漏洞核查尚未完成，正式发布前需要复查。

原 server/.env 保留。验收使用随机生成的 .env.container，密码和配置不进入镜像、Git 或报告。

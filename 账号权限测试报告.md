# 账号与权限测试报告

测试日期：2026-09-04。

## 修复后的最新状态

此前记录的三项代码/数据可见性问题已修复：计划修改统一读取权限配置并检查部门或本人负责范围；执行成员只能修改本人计划的进度和状态；当前团队内本人负责的计划纳入列表、看板、仪表盘和预警的可见范围。

独立测试环境扩展回归 **294/294 通过**。覆盖两个计划修改接口、部门内修改、无关部门拒绝、执行成员分值/负责人/部门/名称/截止日期/参与人修改拒绝，以及本人跨部门计划可见且可修改。旧测试对产业1号位修改无关部门计划的预期也改为 403，使读写范围保持一致。

新入职豁免期仍待确认期限和适用事项，本次未擅自添加豁免规则。下文的 279 项结果和问题列表为修复前的历史记录。

依据：用户提供的《README-部署说明(1).md》第四节演示账号、第七节多团队说明，以及当前项目角色权限配置。MD 没有逐接口权限矩阵，不能把代码中的配置当作文档明确规定。

## 账号与密码

| 账号 | 密码 | 角色 | 团队 | MD 列出 |
|---|---|---|---|---|
| admin | admin123 | 超级管理员 | 万盛股份、涂料事业部 | 是 |
| ceo | ceo123 | CEO | 万盛股份 | 是 |
| zhangwm | 123456 | 产业1号位 | 万盛股份 | 是 |
| wangxp | 123456 | 计划效率部主任 | 万盛股份 | 是 |
| limf | 123456 | 部门1号位（界面显示涂料事业部1号位） | 涂料事业部 | 是 |
| zhaoh | 123456 | 执行成员 | 万盛股份、涂料事业部 | 否，原代码已有 |

以上六组密码均已在当前本地验收服务（9280）实际登录验证，均返回 200。数据库保存的是 bcrypt 哈希，表中密码是经过登录验证的明文输入，并非读取哈希还原。

## 执行范围与结果

写操作使用独立 Compose 项目 peis-clean-test（19280）和独立数据库卷，运行同一应用镜像。测试创建独立计划、战略、会议和奖惩记录，没有修改 9280 验收环境的业务记录。GET 类页面仍可能触发应用自身的预警刷新。

接口及数据断言共 279 项：276 项通过，3 项失败。覆盖全部六个账号的正确/错误密码、团队归属、团队切换、主要模块读取、按角色创建战略/战役/计划/会议/奖惩、周期触发、跨团队记录修改、越权指定团队、计划字段修改。

按账号统计：admin 69/69、ceo 39/39、zhangwm 37/38、wangxp 39/39、limf 33/34、zhaoh 59/60。另用浏览器登录全部六个账号并访问各自菜单中的页面，共 51 次页面访问，没有检测到 JavaScript 异常、加载失败提示或 Promise 文本渲染问题。这不等于所有页面按钮权限均无问题。

测试后已停止独立测试容器，9280 本地验收服务保持运行。

已通过的 MD 明确要求：五个演示账号均存在且密码正确；admin 可访问两个团队；limf 只有涂料事业部；非所属团队请求被拒绝；所测团队数据读取和跨团队修改拦截符合预期。

## 发现的问题

1. **limf 计划编辑权限与接口行为不一致**：部门1号位配置了 `plan.edit`，但修改同部门、由执行成员负责的测试计划返回 403。接口用角色名称正则而非 `plan.edit` 判定修改范围，部门1号位没有纳入。属于当前权限配置和执行逻辑不一致，MD 未明确规定其编辑所有部门计划的范围。
2. **zhaoh 修改字段越权**：执行成员配置只有 `plan.edit_self`，接口注释限定为本人进度，却能通过 PUT 将本人测试计划的 score 改为 999，返回 200。接口没有限制可修改字段。该账号不是 MD 列出的五个账号，但现有权限机制存在此缺口。
3. **zhangwm 看不到自己负责的计划**：种子计划 p_1 的负责人是 zhangwm，组织是企管中心；该账号成员归属为总裁办公室。`/plans?mine=1` 未返回 p_1，因为查询先按部门范围过滤。属于负责人分配与数据可见范围冲突，MD 没有明确规定跨部门负责人可见规则，需要确认业务规则后修复。
4. **limf 新入职豁免期未实现**：MD 明确写了“新入职，享豁免期”。代码仅保存 hireDate；未发现基于入职日期判定豁免及其影响奖惩/预警的逻辑。MD 也没有规定豁免时长、适用对象和豁免事项，不能自行假设规则。此项为代码核查结论，不计入 279 项接口断言。

因此，账号和所测团队隔离符合文档，但不能认定所有角色行为完全符合业务预期，也不能认定文档全部要求已实现。此次仅测试并记录，未修改权限业务逻辑或密码。

## 复现

启动独立测试环境后运行 `node scripts/account-permissions-check.mjs`，默认地址为 19280。该脚本会创建测试数据，不应直接指向正式环境。

详细结果：artifacts/account-permissions-results.json。网页验证脚本：scripts/accounts-browser-check.mjs，结果写入 artifacts/accounts-browser-results.json。
